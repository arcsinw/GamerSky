<Page
    x:Class="游民星空.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:游民星空.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:vm="using:游民星空.Core.ViewModel"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pr="using:PullToRefresh.UWP"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True,Type=vm:MainPageViewModel}"
    >

    <Page.Resources>
        <vm:MainPageViewModel x:Name="MVM"/>
        <x:String x:Key="ErrorString">Error</x:String>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Pivot DataContext="{Binding Source={StaticResource MVM}}"
               ItemsSource="{Binding Channels}" SelectionChanged="Pivot_SelectionChanged">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding nodeName}"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <Pivot.ItemTemplate>
                <DataTemplate>
                    <PivotItem Margin="0" Padding="0">
                        <!--<pr:PullToRefreshBox RefreshInvoked="PullToRefreshBox_RefreshInvoked" >-->
                        <ListView Background="Gray" DataContext="{Binding Source={StaticResource MVM}}"
                                  ItemsSource="{Binding Essays}" Padding="0" Margin="0"
                                  IsItemClickEnabled="True"
                                  ItemClick="ListView_ItemClick"
                                  ContainerContentChanging="ListView_ContainerContentChanging"
                                  >
                                <ListView.HeaderTemplate>
                                    <DataTemplate>
                                        <FlipView Height="200">
                                            <FlipView.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid>
                                                        <Image Height="200" Stretch="UniformToFill">
                                                            <Image.Source>
                                                                <BitmapImage UriSource="{Binding}"/>
                                                            </Image.Source>
                                                        </Image>
                                                        <TextBlock Text="{Binding }"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </FlipView.ItemTemplate>
                                        </FlipView>
                                    </DataTemplate>
                                </ListView.HeaderTemplate>

                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="3*"/>
                                            </Grid.ColumnDefinitions>

                                            <Image Grid.Column="0" HorizontalAlignment="Left">
                                                <Image.Source>
                                                    <BitmapImage UriSource="{Binding thumbnailURLs[0]}"/>
                                                </Image.Source>
                                            </Image>

                                            <Grid Grid.Column="1" HorizontalAlignment="Right" Margin="5,2">
                                                <StackPanel>
                                                    <TextBlock Text="{Binding title}" />
                                                    <TextBlock Text="{Binding readingCount}"/>
                                                    <TextBlock Text="{Binding commentsCount}"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>

                                    </DataTemplate>
                                </ListView.ItemTemplate>

                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                        <Setter Property="Margin" Value="0,5"/>
                                        <Setter Property="Padding" Value="0"/>
                                        <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                            </ListView>
                        <!--</pr:PullToRefreshBox>-->
                    </PivotItem>
                </DataTemplate>
            </Pivot.ItemTemplate>
        </Pivot>

        <Grid>
            <ProgressRing Visibility="{Binding}" Height="60" Width="60" Background="Transparent"/>
        </Grid>
    </Grid>
</Page>
